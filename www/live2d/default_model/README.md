This folder is a minimal Live2D v2 development sample copied from the local
`live2d_demo` reference so the dancer page can render a real Live2D model
without pulling the full 140 MB model library.

Product direction: replace this `default_model` slot with an original black
long-haired ponytail character.

Do not treat this sample as the final product character or commercial asset.
